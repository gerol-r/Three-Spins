<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= song.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/profiles.css" rel="stylesheet">
    <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap"
    rel="stylesheet">
  </head>
  <body>
    <div class="nav-row">
      <div class="col-3">
        <img id="logo" src="/design_collateral/Three-Spins-Logo.png" alt="Three Spins Logo">
      </div>
      <div class="col"></div> <!-- Empty column for spacing -->
      <div class="col-4 nav-col">
        <%- include('../partials/_mainnav.ejs') %> 
      </div>
      <div class="col-4"></div> <!-- Empty column for spacing -->
    </div>
  
    <div class="show-container">
      <div class="user-profile">
        <h4><%= user.username %>'s <%= song.rankText %></h4>
        <img src="<%= user.avatar %>" alt="User Avatar">
      </div>
  
      <h1><%= song.title %> by <%= song.artist %></h1>
  
      <% if (song.comment) { %>
        <p>Why: <%= song.comment %></p>
      <% } %>
  
      <% if (song.mp3Url) { %>
        <a href="<%= song.mp3Url %>">View job posting</a>
      <% } %>
  
      <a href="/users/<%= user._id %>/profile/<%= song._id %>/edit">Edit this song</a>
  
      <form action="/users/<%= user._id %>/profile/<%= song._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete this song</button>
      </form>
    </div>
  </body>
</html>
